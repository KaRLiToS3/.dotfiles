FROM alpine:latest

WORKDIR /app

COPY . /app

RUN apk add --no-cache python3 py3-pip \
    && pip3 install --upgrade pip \
    && pip3 install -r requirements.txt
    I get the error that the environment is externally managed, perhaps the RUN command is no right
# Use a shell script to run both applications
COPY start.sh /app/start.sh
RUN chmod +x /app/start.sh
CMD ["/app/start.sh"]
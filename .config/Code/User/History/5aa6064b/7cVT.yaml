services:
  mosquitto:
    image: eclipse-mosquitto:latest
    ports:
      - "2000:1883"
    volumes:
      - ./mosquitto/config:/mosquitto/config
  
  esp32-server:
    build: ./server
    depends_on:
      mosquitto:
        condition: service_healthy
